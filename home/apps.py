from django.apps import AppConfig


class HomeConfig(AppConfig): #here take the HomeConfig copy this and paste it inside the settings 
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'home'
